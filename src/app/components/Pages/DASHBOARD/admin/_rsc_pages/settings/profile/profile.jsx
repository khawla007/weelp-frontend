import React from 'react';
import { PageInfo } from '../settings_shared';

const Profile = () => {
  return (
    <div className="space-y-6">
      <PageInfo pageTitle={'Profile Settings'} pageDescription={'Manage your profile information and notification preferences'} />
    </div>
  );
};

export default Profile;
